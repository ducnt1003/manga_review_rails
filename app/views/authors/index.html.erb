<h1>Authors</h1>

<table class="table">
  <thead class="table-dark fs-5">
    <tr>
      <th>Name</th>
      <th>Birthday</th>
      <th>Story</th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= link_to author.name, author, class: "btn btn-outline-dark" %> </td>
        <td><%= link_to author.birthday.strftime("%d/%m/%Y"), "#", class: "btn btn-outline-dark" %> </td>
        <% if author.story != "" %>
          <td><%= link_to author.story, "#", class: "btn btn-outline-dark text-break" %> </td>
        <% else %>
          <td></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path, class: "btn btn-outline-success" %>
